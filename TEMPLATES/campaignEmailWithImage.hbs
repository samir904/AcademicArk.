
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{subject}}</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            background-color: #f6f9fc;
        }
        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            border-radius: 16px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.07);
            overflow: hidden;
        }
        .header-image {
            width: 100%;
            display: block;
            max-height: 300px;
            object-fit: cover;
        }
        .content {
            padding: 40px;
        }
        .logo {
            width: 60px;
            height: 60px;
            margin-bottom: 20px;
            border-radius: 12px;
        }
        h1 {
            margin: 0 0 20px;
            color: #1a1a1a;
            font-size: 28px;
            font-weight: 700;
            line-height: 1.3;
        }
        p {
            margin: 0 0 15px;
            color: #4a5568;
            font-size: 16px;
            line-height: 1.6;
        }
        .button {
            display: inline-block;
            padding: 14px 32px;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: #ffffff !important;
            text-decoration: none;
            font-weight: 600;
            font-size: 16px;
            border-radius: 8px;
            margin: 30px 0;
            box-shadow: 0 4px 14px rgba(102, 126, 234, 0.4);
            transition: all 0.3s ease;
        }
        .button:hover {
            transform: translateY(-2px);
            box-shadow: 0 6px 20px rgba(102, 126, 234, 0.6);
        }
        .feature-row {
            display: flex;
            gap: 20px;
            margin: 30px 0;
            padding: 20px;
            background-color: #f7fafc;
            border-radius: 12px;
            border-left: 4px solid #667eea;
        }
        .feature-icon {
            font-size: 32px;
            min-width: 40px;
        }
        .feature-text h3 {
            margin: 0 0 8px;
            color: #1a1a1a;
            font-size: 16px;
            font-weight: 600;
        }
        .feature-text p {
            margin: 0;
            color: #718096;
            font-size: 14px;
        }
        .footer {
            padding: 30px 40px;
            background-color: #f7fafc;
            border-top: 1px solid #e2e8f0;
            text-align: center;
            font-size: 12px;
            color: #718096;
        }
        .divider {
            margin: 30px 0;
            border: 0;
            border-top: 1px solid #e2e8f0;
        }
        @media (max-width: 480px) {
            .content {
                padding: 20px;
            }
            h1 {
                font-size: 24px;
            }
            .feature-row {
                flex-direction: column;
                align-items: center;
                text-align: center;
            }
        }
    </style>
</head>
<body>
    <table width="100%" cellpadding="0" cellspacing="0" role="presentation" style="background-color: #f6f9fc; padding: 20px 0;">
        <tr>
            <td align="center">
                <table class="email-container" width="600" cellpadding="0" cellspacing="0" role="presentation">
                    
                    {{#if headerImage}}
                    <!-- Header Image (Like Spotify banners) -->
                    <tr>
                        <td>
                            <img src="{{headerImage}}" alt="{{subject}}" class="header-image" style="width: 100%; display: block;">
                        </td>
                    </tr>
                    {{/if}}

                    <!-- Content -->
                    <tr>
                        <td class="content">
                            {{#if logo}}
                            <img src="{{logo}}" alt="AcademicArk" class="logo">
                            {{/if}}

                            <h1>{{subject}}</h1>
                            
                            <div style="color: #4a5568; font-size: 16px; line-height: 1.6; margin-bottom: 30px;">
                                {{{message}}}
                            </div>

                            {{#if features}}
                            <hr class="divider">
                            <h2 style="color: #1a1a1a; font-size: 20px; font-weight: 600; margin: 0 0 20px;">✨ What's Included</h2>
                            {{#each features}}
                            <div class="feature-row">
                                <div class="feature-icon">{{this.icon}}</div>
                                <div class="feature-text">
                                    <h3>{{this.title}}</h3>
                                    <p>{{this.description}}</p>
                                </div>
                            </div>
                            {{/each}}
                            {{/if}}

                            {{#if ctaText}}
                            <center>
                                <a href="{{ctaLink}}" class="button">{{ctaText}}</a>
                            </center>
                            {{/if}}
                        </td>
                    </tr>

                    <!-- Footer -->
                    <tr>
                        <td class="footer">
                            <p style="margin: 0 0 10px;">
                                © 2025 AcademicArk. All rights reserved.
                            </p>
                            <p style="margin: 0;">
                                <a href="{{unsubscribeLink}}" style="color: #667eea; text-decoration: none;">Unsubscribe</a> • 
                                <a href="{{preferencesLink}}" style="color: #667eea; text-decoration: none;">Preferences</a>
                            </p>
                        </td>
                    </tr>

                </table>
            </td>
        </tr>
    </table>
</body>
</html>
